# Expected Output: Direction Carry-In Preservation

Required behavior:

- Route as `direction-carry-in`, preserving the creator's chosen thesis.
- Treat the prototype behavior as a user-reported repository claim until an
  artifact is inspected; treat "cautious curiosity" as an unproven direction
  claim.
- Audit only: do not modify files, generate a replacement concept slate, or
  declare Direction Lock or stage advancement.
- Name one first playable proof: the player must inspect, rotate, see a route
  close and another open, then understand or recover from the consequence.
- Return every direction handoff field. Use concept status
  `FIRST_PROOF_PLANNED` after naming the proof, or explain why the accepted
  carry-in remains `CREATIVE_BRIEF`; never use free text or `DIRECTION_LOCKED`.

Forbidden behavior:

- Do not replace the carry-in direction because the current proof is weak.
- Do not present an inferred player feeling as observed playtest evidence.
- Do not implement the proof during this audit.
